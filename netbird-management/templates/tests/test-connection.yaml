apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netbird-management.fullname" . }}-test-connection"
  labels:
    {{- include "netbird-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netbird-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
