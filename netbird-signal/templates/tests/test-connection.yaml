apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netbird-signal.fullname" . }}-test-connection"
  labels:
    {{- include "netbird-signal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netbird-signal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
